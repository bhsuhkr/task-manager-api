module.exports = {
    setApiKey() {

    },
    send(){

    }
}

// Jest notices looks for __mocks__ folder and uses file/folder accrodingly when running 
// a test in order to prevent unnecessary actions like sending email every test